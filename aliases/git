alias gc="git checkout"
alias gcm="git checkout master"
alias gc-="git checkout -"

alias gm="git commit -m"

alias gp="git push"

alias gl="git pull"
alias glr="git pull --rebase"

alias gs="git status"

alias gm="git merge"

alias gr="git rebase"

alias gb="git branch"

alias gd="git diff"
